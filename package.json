{
  "name": "platform-vehicle-defense",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "vitest run",
    "preview": "vite preview",
    "qa:art": "node scripts/qa-art.mjs",
    "qa:audio": "node scripts/qa-audio.mjs",
    "qa:release": "npm run qa:art && npm run qa:audio && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.1.4",
    "vitest": "^1.6.0"
  }
}
