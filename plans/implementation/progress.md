# Implementation Progress

구현 전 대상 plan directory와 이 기록을 확인하고, 구현 후 같은 형식으로 진행 내용을 추가한다.

| 날짜 (yyyy-mm-dd) | plan dir | 진행내용 |
| --- | --- | --- |
| 2026-09-10 | plans/implementation/1-core-gameplay/ | 기존 00~07을 1.1~1.8 세부 plan으로 정리하고 구현 진행 추적을 시작한다. |
| 2026-09-10 | plans/implementation/2-design-art/ | 기존 10~19를 2.1~2.9 세부 plan으로 정리하고 공통 art 계약부터 진행한다. |
| 2026-09-10 | plans/implementation/3-sound/ | 기존 20~22를 3.1~3.2 세부 plan으로 정리하고 라이선스·효과음·배경음악 순서를 유지한다. |
| 2026-09-10 | plans/implementation/4-gameplay-expansion/ | 기존 25.1~25.4를 4.1~4.4로 정리하고 월드·이동·Armory·모듈 배치 순서를 유지한다. |
| 2026-09-10 | plans/implementation/5-terrain-world-map/ | 기존 26.1~26.9를 5.1~5.9로 정리하고 지형·메뉴·월드맵·통합 회귀 순서를 유지한다. |
| 2026-09-10 | plans/implementation/6-map-art-terrain/ | 기존 27.1~27.6을 6.1~6.6 세부 plan으로 분리하고 맵 이미지·지형·차체 상호작용을 추적한다. |
| 2026-09-11 | plans/implementation/7-map-tile-terrain/ | 1번 맵을 18px tile rows 기반 160x120 지형으로 전환하고, 4개 enemy spawn marker·숨은 tank start·아트 정렬 QA·실제 브라우저 통합 검증(PASS)까지 7.1~7.10을 완료했다. |
| 2026-09-11 | plans/implementation/7-map-tile-terrain/ | 7.11에서 1번 맵 탱크 terrain hitbox를 3 tile 폭 수준으로 축소하고 좁은 연결부를 1 tile 확장했으며, 자동 QA와 실제 브라우저 검증(PASS)을 완료했다. |
| 2026-09-11 | plans/implementation/7-map-tile-terrain/ | 7.11.8에서 map1 terrain hitbox를 0.45(약 2.7 tile)로 완화하고 중심선 오차 통로 회귀 테스트를 추가했다. `npm run qa:release`는 45 tests/QA/build/audio 모두 통과했으며, 통합 QA는 REGION CLEARED 전환으로 실제 통로 완료 검증이 BLOCKED였다. |
| 2026-09-12 | plans/implementation/7-map-tile-terrain/ | 7.11-B1에서 map1 tank terrain hitbox를 원형으로 전환하고 corner clearance·열린 방향 이동 회귀 테스트, loader/아트 계약과 F3 원형 overlay를 추가했다. `npm run qa:release`는 47 tests/QA/build/audio 통과, 공식 integration-tester는 `http://localhost:5175/`에서 canvas focus 후 WASD 이동·원형 판정·콘솔 오류 없음을 확인해 PASS했다. |
| 2026-09-12 | plans/implementation/8-enemy-spawn-scaling/ | `enemies.json` 루트 spawn 정책, 데이터 검증, 순수 threat/batch/interval 계산, WaveManager batch 생성과 회귀 테스트·적 콘텐츠 가이드를 구현했다. `npm run qa:release`는 52 tests/QA/build/audio 통과, integration-tester는 `http://localhost:5176/`에서 Map 1 생성·Wave 진행·HUD·지형·이동·콘솔을 확인해 PASS했다. 기본 정책의 batch/interval 보정값은 기존 동작 보존을 위해 0으로 유지하고 scaling은 정책 주입 테스트로 검증했다. |
| 2026-09-12 | plans/implementation/8-enemy-spawn-scaling/ | 사용자 조정으로 공통 적 spawn batch를 5, interval 배율을 0.5로 적용하고 최소 interval·초과 생성 방지·검증 문서를 갱신했다. `npm run qa:release`는 53 tests/QA/build/audio 통과, plan runtime 검증은 test map의 Wave 1·2 스폰·HUD·콘솔 오류 없음을 확인했으며 차량 이동은 변경 범위 외라 `SKIP-N/A`로 기록했다. |
| 2026-09-13 | plans/implementation/09-ui-improvement/ | Subtask 1~5로 production snapshot, deterministic production 상태 fixture, 탱크 상단 Core HP, 4개 production cycle bar, 확대 시스템 card, compact upgrade map, hover tooltip, armory/terminal fixture를 구현했다. baseline runtime `35142` (`http://127.0.0.1:5179/?test=1`)과 fixture runtime `49537` (`http://127.0.0.1:5181/`)의 Plan 통합 검증에서 UI-09-1/2/3/4/5/6/7/8/10과 1280x720 layout PASS, 이동 hold·축소 viewport는 API 부재로 `SKIP-ENV`, VICTORY는 실제 production transition 부재로 `SKIP-N/A` 처리했다. fixture의 WAIT INPUT/BUFFER FULL/STORAGE FULL, armory install·drag·R 회전, GAME_OVER/REGION_CLEARED가 재현됐고 콘솔 errors/warnings는 0이다. 사용자 요청에 따라 자동 테스트/build/qa:release는 실행하지 않았다. |
| 2026-09-13 | plans/implementation/09-ui-improvement/ | PR #14 merge 후 Pages build의 TS6133 오류를 확인하고 `HUDManager` tooltip target reference를 정리하는 후속 fix를 시작했다. 성공 게이트는 Pages live URL의 production cycle bar와 탱크 위 Core HP bar 확인이며, 모바일 검증은 범위에서 제외한다. |
| 2026-09-13 | plans/implementation/09-ui-improvement/ | PR #15 merge 후 Pages run `34747820331` 성공을 확인했다. 기본 `https://dahoon-hong.github.io/` 전투 화면에서 4개 production cycle bar와 탱크 위 `HP 100/100` bar를 확인해 Pages UI 성공 게이트를 PASS 처리했다. 사용자 범위에 따라 모바일 검증은 실행하지 않았다. |
| 2026-09-12 | plans/implementation/plan-completion-testing/ | PR 전 중복 통합테스트를 plan 완료 시점 테스트로 전환하고, 지정 worktree·strict port runtime launcher, dev-only observer, canvas focus·입력/이동/wave telemetry를 추가했다. `qa:release`는 51 tests/QA/build/audio 통과, runtime 충돌은 자동 port 변경 없이 실패, 실제 runtime 검증은 observer·게임플레이·wave/spawn·console PASS 및 단발 key press 환경의 이동만 SKIP-ENV로 기록하고 runtime/port/manifest cleanup을 확인했다. |
| 2026-09-13 | plans/implementation/10-enemy-spawn-weight/ | 사용자 정정에 맞춰 웨이브 타입별 quota와 공통 spawn 정책을 제거하고, `spawn × 적별 spawnWeight` 기반의 타입별 interval·batch 연속 생성과 `targetKills` 처치 목표를 구현했다. `npm test` 62개, art QA 오류 0, build, diff check와 `http://127.0.0.1:5185/?test=1` runtime의 observer·연속 생성·Wave 2·HUD·콘솔 검증을 통과했다. |
| 2026-09-13 | plans/implementation/11-enemy-pathfinding-performance/ | `EnemyNavigationCoordinator`의 공통 경로 요청 dedupe·bounded LRU/negative cache·주기/프레임 budget·terrain reset·DEV telemetry를 구현하고 적 직접 A*를 제거했다. `npm test` 73개, `npm run build`, `npm run qa:release`는 통과했으며, 첫 runtime 통합 검증에서 observer budget/idle/reset/console은 통과했지만 이동 입력 제약으로 필수 시나리오 1·4가 실패했다. DEV 전용 자동 이동 시나리오를 추가한 뒤 최종 runtime 검증은 사용자 지시로 생략했다. |
| 2026-09-14 | plans/implementation/09-a-enemy-local-steering/ | 차량 engagement slot·local steering(seek/arrival/separation/terrain probe)·hysteresis·stuck priority repath와 적별 DEV telemetry/fixture를 구현했다. `npm test` 77개, `npm run qa:release`(art errors 0/audio/build 포함), `git diff --check`를 통과했고, 사용자가 runtime QA 화면을 직접 확인해 `MANUAL-PASS`로 승인했다. 자동 integration-tester는 browser/runtime 관측 계약 timeout으로 완료되지 않아 반복하지 않았다. |
| 2026-09-14 | plans/implementation/12-enemy-navigation-worker-priority/ | 구현 전 개발 편의를 위해 `aurelia/landing-zone`을 canonical test map으로 전환하고 test-map 방어력 100을 적용했다. 기존 `test/terrain-test` 데이터·아트·생성 스크립트를 폐기하고 나머지 맵은 시나리오 맵으로 유지했으며, 테스트 78개·build·art QA와 runtime 검증(PASS)을 완료했다. |
| 2026-09-14 | plans/implementation/12-enemy-navigation-worker-priority/ | 단일 Web Worker batch A*·stale revision 검증·동기 fallback·시야 우선 local/engaged 추적·blocked priority 요청·spatial grid·direct terrain 결과 재사용·DEV telemetry를 구현했다. `npm run qa:release`는 테스트 87개, art/audio/build 통과(art 기존 warning 2개, error 0), `git diff --check` 통과. canonical map/방어력 100/고정 적 160명 fixture에서 Worker-on 약 6분 runtime은 `GAMEPLAY/PLAYING`, 이동 증가, `stuck=0`, `pending=0`, queue=0, fallback=0, console error/warn=0으로 PASS했다. 안정 구간 navigation main time은 약 3.2ms였고 누적 max 95.4ms 단발 outlier가 남아 후속 profiling 대상으로 기록한다. Worker-off baseline은 동작·콘솔은 통과하되 초기 `pendingNavigationAgents=146`, `navigationMainMsMax≈32.3ms`를 확인했다. |
| 2026-09-14 | plans/implementation/12-enemy-navigation-worker-priority/ | 장애물 옆에서 경로 탐색이 `null`을 반환한 뒤 negative cache가 영구 고착되던 문제를 수정했다. null cache에 `repathInterval` TTL을 적용하고, 새 null 결과에서 다음 engagement slot을 재탐색하도록 해 적 정지를 회복한다. `npm run qa:release`는 테스트 88개, art/audio/build 통과(art 기존 warning 2개, error 0), `git diff --check` 통과. 지정 Worker-on runtime은 canonical map/방어력 100에서 이동거리 `73 → 8,077`, `stuck=0`, `pending-path=0`, `fallback=0`, GAME_OVER 없음, console `[]`를 직접 확인했다. integration-tester는 console API 부재로 BLOCKED 처리했다. |
| 2026-09-14 | plans/implementation/12-enemy-navigation-worker-priority/ | 차량 밀침으로 적이 벽에 끼이는 문제와 spawn 군집 겹침을 후속 보정했다. `EnemyCollisionResolver`가 차량 push를 enemy terrain-safe 위치로 클리핑하고, 기존 `EnemySpatialIndex` 기반으로 이동 전·후 적 간 겹침을 bounded pass로 분리한다. exact-overlap spawn도 결정론적으로 펼친다. `npm run qa:release`는 테스트 92개, art/audio/build 통과(art 기존 warning 2개, error 0), `git diff --check` 통과. 자동 integration-tester는 4분 내 결과 미반환으로 BLOCKED였고, 동일 원인 재시도 대신 Chrome runtime `http://127.0.0.1:5194/?test=1&scenario=enemy-navigation-worker&worker=on`에서 canonical map/방어력 100/160적을 직접 확인해 MANUAL-PASS했다. 초기·이동·안정 구간의 observer 샘플에서 최소 적 간 거리는 24.02px 이상, terrainUnsafe 0, stuck 0, GAME_OVER 없음, Worker fallback 0으로 확인했다. |
| 2026-09-14 | plans/implementation/13-armory-research/ | Armory 연구를 재생 중에도 허용하도록 `HUDManager`의 일시정지 차단 조건을 제거했다. `armory-install` runtime에서 PLAYING 상태 Mortar 연구와 연구 후 구매가 모두 성공했으며, `npm test` 92개·`npm run build`·`git diff --check`를 통과했다. |
| 2026-09-14 | plans/implementation/12-enemy-navigation-worker-priority/ | 12.7~12.9을 구현했다. 적-적 직접 위치 보정을 제거하고 coordinator의 bounded neighbor snapshot에서 velocity push intent를 계산하며, 실제 terrain-safe 이동에 합산했다. spawn admission·skip cursor·포화 skip telemetry, standard/tanker collider radius 10/15, nearest bounded query, collision p95와 160/320/640 stress fixture를 추가했다. `npm run qa:release`는 테스트 95개, art errors 0(기존 warning 2개), audio/build 통과했고 `git diff --check`도 통과했다. 브라우저 자동화는 Windows ACL 오류로 실행할 수 있어 필수 runtime QA는 미실행 상태다. |
| 2026-09-14 | plans/implementation/15-modern-firearms-tank/ | 승인된 현대 화기 9종, 독립 Armory 연구/구매 stock, 계열별 크기·사격각·최소거리·탄창/재장전, Standard/Tanker armor와 관통/다중 관통/전차포 폭발/곡사 탄도, 공유 EnemySpatialIndex, 고유 generated body/icon·procedural fire sound·FX/recoil을 구현했다. DEV `modern-firearms-stress` fixture는 120 active Standard/Tanker 적과 다중 발사를 제공한다. `npm run qa:release`는 21 files/105 tests, art errors 0(신규 generated asset draw-size 및 기존 unlisted asset warning 22개), audio/build 통과, `git diff --check` 통과. 지정 runtime `http://127.0.0.1:5201/?test=1&scenario=modern-firearms-stress&count=120&worker=on`에서 observer·120명·armor 10/40·5개 설치 무기·발사체·재장전·HP 감소를 확인했으나, integration-tester와 직접 CUA가 Windows `apply deny-read ACLs` helper 오류로 canvas 클릭 흐름과 장시간 spike 구분을 완료하지 못해 필수 runtime QA는 BLOCKED로 기록한다. 제품 FAIL 증거는 없었고, 승인된 성능 fallback은 적용하지 않았다. `/favicon.ico` 404는 기존 기본 요청으로 확인했다. |
| 2026-09-15 | plans/implementation/15-modern-firearms-tank/ | 사용자 정정에 따라 기관총 ammo 소비를 발사당 차감에서 재장전당 1개 차감으로 수정하고, 12.7mm/20mm/30mm 탄창을 20/15/10발로 정정했다. 기관총 재장전 자원 부족 대기와 단발 무기 발사당 ammo 소비 회귀 테스트를 추가했다. |
| 2026-09-15 | plans/implementation/15-modern-firearms-tank/ | 자원별 최대 저장량을 JSON으로 분리하고 `ResourceStorage`가 capacity map을 적용하도록 수정했다. 자원별 capacity와 기존 저장 상한 동작 테스트를 추가했다. |
| 2026-09-15 | plans/implementation/15-modern-firearms-tank/ | 두 레거시 무기 ID를 `machine-gun-12.7mm`/`mortar-60mm`로 전환하고, 연구 노드·JSON 파일명·초기 배치·에셋·문서·테스트 참조를 일괄 정리했다. |
| 2026-09-15 | plans/implementation/15-modern-firearms-tank/ | 모든 combat JSON의 사거리를 `minRange`/`maxRange`로 통일하고, 실제 조준 판정과 전차포·곡사포 설치 미리보기에 최소 안전거리와 최대 사거리를 반영했다. 로더 검증·직사/곡사 회귀 테스트와 관련 문서를 갱신했다. |
| 2026-09-15 | plans/implementation/15-audio-track-integration/ | 사용자 제공 MP3 2개를 승인된 번들 음악으로 등록하고 main menu·canonical test map·기타 gameplay fallback 재생 경로를 연결했다. `npm run qa:release` 최종 확인과 `git diff --check`를 통과했으며, `http://127.0.0.1:5200/?test=1`에서 두 MP3 fetch·test map/기본 map 전환·PLAYING/PAUSED/PLAYING 상태·console errors/warnings 0을 수동 검증했다. 자동 integration-tester는 브라우저 자동화 timeout으로 BLOCKED되어 같은 원인 재시도 없이 수동 검증으로 대체했다. |
| 2026-09-16 | plans/implementation/16-vehicle-ram-collision-damage/ | 차량 실제 이동 전후 snapshot과 `EnemyCollisionResolver` 접촉 이벤트를 연결하고 상대 접근 속도·`dt` 적분 램 피해, armor 무시, Standard/Tanker DEV fixture와 observer telemetry를 구현했다. `npm test` 117개, `npm run build`, `git diff --check`를 통과했다. 지정 runtime `http://127.0.0.1:5223/?test=1&scenario=vehicle-ram` (`runtimeId=plan-16-vehicle-ram-20260916`, worktree `M:\projects\local_game_repo\defence`)에서 CUA 수동 fallback으로 GAMEPLAY/PLAYING, 두 적 HP 감소, `ramContactsThisFrame=2`, 양수 `ramMaxRelativeClosingSpeed≈163.43`, 누적 `ramDamageTotal=13.403`, 이동거리 `184.47`, armor `100`, 두 적 `terrainSafe=true`, 정지 후 누적 피해 유지, console errors/warnings 0을 확인해 필수 runtime을 MANUAL-PASS 처리했다. `enemy-navigation` 일반 전투 smoke도 적 생성·navigation·PLAYING·console 오류/경고 없음으로 확인했다. |
| 2026-09-16 | plans/implementation/14-threat-based-difficulty/ | ThreatManager·threat.json·맵별 threat.baseMultiplier와 MapDefinitionLoader 검증, Stage PLAYING 시간/normal 1.0 입력, Wave batch·targetKills·contactDamage 보정, threat-scaling observer fixture를 구현했다. npm run qa:release와 fixture 보정 후 npm test 125개·npm run build·git diff --check를 통과했다. 공식 integration-tester는 초기·시간 상승·Pause·Wave 2 target snapshot을 관측했으나 bounded timeout으로 최종 console/일반 전투 회귀를 완료하지 못해 runtime QA를 BLOCKED로 기록한다. |
| 2026-09-16 | plans/implementation/14-threat-based-difficulty/ | 사용자 승인에 따라 spawn 전용 보정 없이 공통 threat 시간 수식에 `time.initialMultiplier=0.25`를 추가하고 production `growthMultiplier=1.32`로 조정해 초기 약 1/4 수준에서 약 5분에 기준값으로 회복하도록 했다. `spawnBatch.minMultiplier=0.25`와 설정·경계 테스트를 갱신했으며 `npm run qa:release`(126 tests, art/audio/build 통과, 기존 art warning 20개), `git diff --check`를 통과했다. runtime은 `http://127.0.0.1:5188/?test=1&scenario=threat-scaling`에서 초기 `threatMultiplier=0.225`/`spawnBatchMultiplier=0.25`, 시간 상승·Wave 2 batch 증가, Pause 중 elapsed/threat 고정을 CUA로 확인했다. 공식 integration-tester는 60초 대기 3회에도 결과를 반환하지 않아 shutdown되어 full console/regression 판정은 BLOCKED로 남긴다. |
